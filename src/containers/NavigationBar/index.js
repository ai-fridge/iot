import React, { Component } from 'react';
import NavigationBar from '../../components/NavigationBar';

class NavigationBarContainer extends Component {
  render() {
    return (
      <NavigationBar title="AI Fridge" />
    );
  }
}

export default NavigationBarContainer;
